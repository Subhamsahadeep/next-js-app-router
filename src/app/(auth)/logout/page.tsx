import { AuthExceptionError } from '@/app/libs/exceptions'
import React from 'react'

type Props = {}

export default function logout({}: Props) {
  // throw new AuthExceptionError("Logout not implemented error.. ");

  return (
    <div>logout</div>
  )
}